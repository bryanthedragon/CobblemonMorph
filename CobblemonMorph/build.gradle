plugins {
    id "java"
    id 'dev.architectury.loom' version '1.14.473'
    id 'architectury-plugin' version '3.4-SNAPSHOT'
    id 'maven-publish'
}

architectury {
    platformSetupLoomIde()
    forge()
}

loom {
    silentMojangMappingsLicense()
    mixin {
        defaultRefmapName.set("mixins.${project.name}.refmap.json")
    }
}

repositories {
    mavenCentral()
    maven { url = "https://dl.cloudsmith.io/public/geckolib3/geckolib/maven/" }
    maven { url = "https://maven.impactdev.net/repository/development/" }
    maven { url = "https://hub.spigotmc.org/nexus/content/groups/public/" }
    maven { url = "https://thedarkcolour.github.io/KotlinForForge/" }
}

dependencies {
    // Minecraft + Forge handled by Loom
    minecraft("com.mojang:minecraft:${minecraft_version}")
    mappings loom.officialMojangMappings()

    forge("net.minecraftforge:forge:${minecraft_version}-${forge_version}")

    // Cobblemon and required local mod jars
    modImplementation(files("libs/cobblemon-forge-1.5.2-1.20.1.jar"))
    modImplementation(files("libs/remorphed-4.2.jar", "libs/craftedcore-5.8.2.jar", "libs/walkers-5.8.11.jar"))

    // KotlinForForge local runtime dependency
    runtimeOnly(files("libs/kotlinforforge-4.12.0-all.jar"))

    // Testing
    testImplementation("org.junit.jupiter:junit-jupiter-api:5.10.0")
    testRuntimeOnly("org.junit.jupiter:junit-jupiter-engine:5.10.0")
}

base {
    archivesName = mod_id
}

java {
    toolchain.languageVersion = JavaLanguageVersion.of(17)
}

processResources {
    filesMatching("META-INF/mods.toml") {
        expand project.properties
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}

jar {
    manifest {
        attributes(
            "Specification-Title": mod_id,
            "Specification-Vendor": mod_authors,
            "Specification-Version": "1",
            "Implementation-Title": project.name,
            "Implementation-Version": project.version,
            "Implementation-Vendor": mod_authors
        )
    }
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifact jar
        }
    }
}

tasks.named("runClient") {
    jvmArgs = ["-Xmx4G", "-XX:+UseG1GC"]
}

group = mod_group_id
version = mod_version

// dependencies {
//     minecraft "net.minecraftforge:forge:${minecraft_version}-${forge_version}"

//     // Example mods (ONLY keep what you actually use)

//     implementation fg.deobf("curse.maven:cobblemon-687131:5375390") // 1.5.2
//     implementation fg.deobf("curse.maven:remorphed-950721:5755676") // 4.2

//     // Remorphed dependency
//     implementation fg.deobf("curse.maven:woodwalkers-868922:7776200") // 5.8.11

//     // Walker dependency
//     implementation fg.deobf("curse.maven:crafted-core-923377:7575385") // 5.8.2

//     // Cobblemon dependency
//     implementation fg.deobf("curse.maven:kotlin-for-forge-351264:7291067") // 4.12.0
// }

// repositories {
//     mavenCentral()

//     // REQUIRED for most mods
//     maven { url = "https://www.cursemaven.com" }

//     // Optional common mod libs
//     maven { url = "https://repo.spongepowered.org/maven" }
//     maven { url = "https://maven.thedarkcolour.github.io/KotlinForForge/" }
//     maven { url = "https://maven.terraformersmc.com/" }
// }

